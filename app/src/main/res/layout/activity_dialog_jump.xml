<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".lifecycle.dialogactivityjump.DialogActivityJumpActivity">

    <TextView
        android:id="@+id/jump"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="Activity jump Dialog Activity"
        android:onClick="onClick"/>

    <TextView
        android:id="@+id/dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/jump"
        app:layout_constraintLeft_toLeftOf="@+id/jump"
        app:layout_constraintRight_toRightOf="@+id/jump"
        android:layout_marginTop="50dp"
        android:text="显示dialog"
        android:onClick="onClick"/>

</android.support.constraint.ConstraintLayout>